-- 코드를 입력하세요
WITH RECURSIVE TMP AS(
    SELECT 0 AS H
    UNION ALL
    SELECT H+1
    FROM TMP
    WHERE H < 23
)
SELECT TMP.H AS HOUR, COUNT(HOUR(A.DATETIME)) AS COUNT
FROM TMP
LEFT OUTER JOIN ANIMAL_OUTS AS A
ON HOUR(A.DATETIME) = TMP.H
GROUP BY HOUR
ORDER BY TMP.H