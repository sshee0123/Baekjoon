def solution(phone_number):
    len_num = len(phone_number)
    arr = list(phone_number)
    for i in range(len(arr)):
        if i == len_num-4:
            break
        arr[i] = '*'
    return ''.join(arr)