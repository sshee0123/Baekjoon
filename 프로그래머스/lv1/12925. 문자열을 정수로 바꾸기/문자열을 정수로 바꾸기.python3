def solution(s):
    result = 0
    if s[0] == '-':
        num = s[1:]
        result = int(num)*(-1)
    elif s[0] == '+':
        num = s[1:]
        result = int(num)
    else:
        result = int(s)
    return result